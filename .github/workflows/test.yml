on:
  push:
  pull_request:
  workflow_dispatch:

name: Run tests

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Set up git repo
        uses: actions/checkout@v2

      - name: Test
        run: bash ./scripts/test.sh
